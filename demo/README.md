# demo/

Contains front-end scripts and demo assets.

- `app.js` — Main controller wired to `index.html` (language select, theme select, run, copy, clear)

To add new demos, create a new `.html` in the project root and a corresponding `.js` here.
